# 复审记录：答疑与记忆设计（v1.0 → v1.2 定稿）

审阅方式：R1（一致性/准确性视角）与 R2（事实核查/记忆架构可行性视角）并行独立子代理审阅，修订后 R3 验证轮（+1）确认，均不采信采纳自报。

## R1 发现与采纳（一致性视角，0 blocker + 3 major + 5 minor）

| ID | 严重度 | 问题 | 采纳 |
| --- | --- | --- | --- |
| G1 | major | OV 调用点三处口径不一（图 S1/S3、价值段 S3/S4、附录仅 S1+月度回看） | 采纳：统一为 S1 调研 + 月度回看两个工具级调用点，S3 写手不接 OV，四处同步 |
| G2 | major | 「新检查可零代码上线」与主方案出口①（新增 check_*.py 断言）矛盾 | 采纳：改「新增断言上线，须拍板」，不动栏改「既有脚本行为不变」（兼 m2） |
| G3 | major | 「五类」与六行表矛盾、5.2 图漏参数记忆、过程记忆是否 L4 未说明 | 采纳：改六类、注明前五类在 L4、过程记忆属流程盘面 |
| m1 | minor | /polish「三道工序」不实（实为四阶段、两道改写工序） | 采纳改「四阶段流程（B/C 为两道改写工序）」 |
| m2 | minor | 机核行不动栏与出口①自相矛盾 | 并入 G2 |
| m3 | minor | 「主方案有意不碰」系盘点代拟 | 改「主方案未涉及的」 |
| m4 | minor | 对话模式 OV hooks 注入未排除 R 轮审稿子代理 | 采纳：R 轮不消费 OV 注入（对齐 T2） |
| m5 | minor | 85% 引述盘外无法核验 | 并入 F2 更正并标注出处 |

## R2 发现与采纳（事实/可行性视角，0 blocker + 3 major + 4 minor）

已核实为准确：--tools 白名单（args.ts:137、subagent index.ts:307）、createAgentSession 构造参数（sdk.ts:39-88）、pi-ai compat 层、独立子进程（--mode json -p --no-session）、OV 四张公众号记忆卡实存、对话/跑批边界。

| ID | 严重度 | 问题 | 采纳 |
| --- | --- | --- | --- |
| F1 | major | 官方 reviewer.md 白名单含 bash（自述权限非完全可强制），「直接抄到物理只读」夸大 | 采纳：明示官方示例含 bash，落地须按主方案 §3.1 显式收窄 read/grep/ls |
| F2 | major | 「85%/2 硬差距」误引：出处 pi-完全替代ZCode方案报告.md:11，2 硬差距=桌面 GUI+浏览器（方向 pi 替代 ZCode），非「宿主可控性/模型自由度」 | 采纳：更正引述并注明维度不同不冲突 |
| F3 | major | 圈2 D1/D4 均已拍板且互补（D1=SDK 嵌入形态、D4=a-pi-space 独立仓+npm 锁版本），原文写成「之争」、D4 误为「加法式 fork 升级」 | 采纳：按拍板内容改写衔接段 |
| F4 | minor | tag/指针形式未定 | 采纳：tag 固定 harness:article-system，OV 条目写仓内路径+主方案版本指针 |
| F5 | minor | 上行 OV 未挂拍板 | 采纳：拟上行清单随月度回看呈用户，拍板后写 |
| F6 | minor | 超时/降级未定义 | 采纳：10 秒超时（config 可调）、跳过并在调研包留痕 |
| F7 | minor | 注入内容与盘面冲突无仲裁 | 采纳：AGENTS.md > .harness/spec > OV 注入 |

## R3 验证轮（+1）

逐条核验 16 项全部已修、0 改坏：OV 四处口径一致（仅 S1+月度回看，S3 明确不接）；六类表与 5.2 图集合一致；F2 更正与报告原文逐字相符；F3 与圈2总纲拍板表（T-20260920-02/-05）相符。新发现 2 minor：§四 序数措辞、附录「可选增强位」与「不 import OV」措辞张力——已顺手修掉。

## 结论

**通过，v1.2 定稿**（0 blocker / 0 major / 2 minor 已修）。
